Drop your background track here, named exactly:

    music.mp3

The "music on/off" control in index.html already points at
assets/audio/music.mp3 and will play/pause it automatically
once the file exists. Keep it reasonably short and loop-friendly
since it's set to loop.
